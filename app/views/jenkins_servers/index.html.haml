%h1 
  Jenkins Servers
  = " assigend to #{@current_team.name}" if params[:team_id]

.row
  .span12
  = link_to "Create a new server", new_jenkins_server_path
  %br
  %span= "Change current team: "
  = select_tag :team_id, options_for_select(@user_teams.map {|t| [t.name, t.id] } ), id: 'index_jenkins_servers_team_id', include_blank: true
  %hr
  - if @jenkins_servers
    = render partial: 'shared/jenkins_index', locals: { jenkins_instances: @jenkins_servers, jenkins_server: @jenkins_server }