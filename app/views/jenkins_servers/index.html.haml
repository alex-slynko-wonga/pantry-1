%h1
  Jenkins Servers
  - if @current_team
    assigned to
    = link_to @current_team.name, @current_team
- if can_add_server?(@current_team)
  .row
    .span12= link_to 'Create a new server', new_jenkins_server_path

.row
  .span12
    = render 'listing', jenkins_servers: @jenkins_servers, user_teams: @user_teams, current_team: @current_team
