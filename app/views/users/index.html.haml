%h1 Users of Pantry
%table.table
  %thead
    %tr
      %th #
      %th Username
      %th Email
  - @users.each_with_index do |user, index|
    %tr
      %td= index + 1
      %td= link_to user.username, user_url(user.id)
      %td= user.email
