<%= @app_environment %>:
  access_key_id: <%= @config["aws"]["access_key_id"] %>
  secret_access_key: <%= @config["aws"]["secret_access_key"] %>
  region: <%= @config["aws"]["region"] %>
  request_wait: <%= @config["aws"]["request_wait"] %>